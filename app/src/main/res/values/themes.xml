<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DELIVERYBOY" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Thumb and track tint colors for SwitchMaterial -->
        <item name="colorControlActivated">@color/orange_btn_color</item>
        <item name="colorControlNormal">@color/black</item>

        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar">true</item>

    </style>


    <style name="CustomToolbar" parent="Widget.MaterialComponents.Toolbar">
        <!-- Set your custom font -->
        <item name="android:fontFamily">@font/exo</item>
        <!-- Make the text bold -->
        <item name="android:textStyle">bold</item>
    </style>


    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/bottom_sheet_animation_inside</item>
        <item name="android:windowExitAnimation">@anim/bottom_sheet_animation_outsid</item>
    </style>



</resources>
